package org.ta_selenium.interfaces;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
public interface ScrollAction {
    void execute(WebDriver driver, WebElement element);
}
